[project]
name = "brd-interpretation-pipeline"
version = "0.1.0"
description = "Controlled BRD to Jira-ready artifact interpretation pipeline"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["fastapi", "uvicorn", "pydantic"]
